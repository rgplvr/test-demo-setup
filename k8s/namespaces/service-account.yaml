# Shared service account for all demo services — IRSA annotation set by terraform output
apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo-services
  namespace: demo
  annotations:
    eks.amazonaws.com/role-arn: "${APP_IRSA_ROLE_ARN}"  # replaced by deploy script
