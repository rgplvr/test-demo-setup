# Plain service account for local minikube — no IRSA annotation.
# In EKS: k8s/namespaces/service-account.yaml is used instead (has eks.amazonaws.com/role-arn).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo-services
  namespace: demo
